# EverMemOS MCP Server Dependencies
# Install with: pip install -r mcp_server/requirements.txt
#           or: uv add --group mcp $(cat mcp_server/requirements.txt | tr '\n' ' ')

mcp[cli]>=1.3.0
httpx>=0.27.0
python-dotenv>=1.0.1
